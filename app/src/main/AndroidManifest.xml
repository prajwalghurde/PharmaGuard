<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.miniprojectapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiniProjectApp"
        android:usesCleartextTraffic="true">

        <!-- Splash Screen (Launcher) -->
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/Theme.MiniProjectApp.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".SignUp" android:exported="false" />

        <!-- Dashboard -->
        <activity android:name=".DashboardActivity" android:exported="false" />

        <!-- Scanning -->
        <activity android:name=".BarcodeScanActivity" android:exported="false" />
        <activity android:name=".PhotoScanActivity" android:exported="false" />

        <!-- Detail / History -->
        <activity android:name=".MedicineDetailActivity" android:exported="false" />
        <activity android:name=".ScanHistoryActivity" android:exported="false" />

        <!-- Admin / Report -->
        <activity android:name=".AddMedicineActivity" android:exported="false" />
        <activity android:name=".ReportCounterfeitActivity" android:exported="false" />

    </application>
</manifest>
